<% if (Array.isArray(opt.banners) && opt.banners.length) { %>
  <% const colSize = Math.ceil(12 / opt.banners.length) %>
  <div class="banners-grid-text mt-2">
    <div class="container">
      <div class="row">
        <% for (let i = 0; i < opt.banners.length; i++) { %>
          <div class="col-12 col-lg-<%= colSize %> mb-2 mb-lg-0">
            <%- await include('@/sections/inc/banner-text', { _, opt: opt.banners[i] }) %>
          </div>
        <% } %>
      </div>
    </div>
  </div>
<% } %>
